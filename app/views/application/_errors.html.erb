
<%if item.errors.any?%>
  <div class="fields-with-errors">
    <h2>
      <%= pluralize(item.errors.count, "error")%>
      prevented this <%=item.class.name.downcase%> from being saved:
    </h2>
    <ul>
      <%item.errors.full_messages.each do |m|%>
      <li><%=msg%></li>
      <%end%>
    </ul>
  </div>
<%end%>
